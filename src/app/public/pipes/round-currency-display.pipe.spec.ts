import { RoundCurrencyDisplayPipe } from './round-currency-display.pipe';

describe('RoundCurrencyDisplayPipe', () => {
  it('create an instance', () => {
    const pipe = new RoundCurrencyDisplayPipe();
    expect(pipe).to.be.true;
  });
});
